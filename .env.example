# === 環境變數範本 ===
# 本機開發：複製此檔為 .env，填入下面兩個值（在 Supabase → Settings → API 取得）
# Vercel 部署：到 Project → Settings → Environment Variables 設定同樣兩個變數

# Supabase 專案網址
VITE_SUPABASE_URL=https://你的專案ID.supabase.co

# Supabase publishable（可公開）金鑰，格式為 sb_publishable_...
VITE_SUPABASE_ANON_KEY=sb_publishable_你的金鑰

# LINE 登入用｜LIFF 應用 ID（LINE Developers → 你的 LINE Login channel → LIFF 分頁取得）
# 格式像 1234567890-abcdEfGh。正式站必填；本機 localhost 通常過不了 LIFF，改用下方測試帳號。
# ⚠️ 此 LIFF 所屬 channel 的 Channel ID，要一併設到 Supabase Edge Function 的 LINE_CHANNEL_ID（兩者須同一個 channel）。
VITE_LIFF_ID=你的LIFF_ID

# 選填｜本機開發用：=1 跳過 LINE 登入改用測試帳號（連真 Supabase）。
# 正式部署（Cloudflare Pages / Vercel）請「不要」設這個，或設 0。
# VITE_DEV_PREVIEW=1
